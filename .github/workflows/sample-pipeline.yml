name: Sample Pipeline
on: [push]
jobs:
  MyFirstPipeline:
    runs-on: ubuntu-latest
    steps:
    - name: Print Hello
      run: |
        echo "Hello Pipeline"
        ls -la 
        cat /etc/proc